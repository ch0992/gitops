# Default values for rag-fastapi helm chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# 환경 설정
environment:
  name: "" # 필수: local 또는 github

replicaCount: 1

image:
  repository: ""
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  # port는 반드시 환경별 values 파일(values-local.yaml 또는 values-github.yaml)에서 정의해야 합니다
  # local: 8060
  # github: 8061
  port: null

resources:
  limits:
    cpu: 1000m
    memory: 2Gi
  requests:
    cpu: 500m
    memory: 1Gi

probes:
  livenessProbe:
    httpGet:
      path: /
      port: 8000
    initialDelaySeconds: 120
    periodSeconds: 30
    timeoutSeconds: 10
    successThreshold: 1
    failureThreshold: 5
  readinessProbe:
    httpGet:
      path: /
      port: 8000
    initialDelaySeconds: 60
    periodSeconds: 5
    timeoutSeconds: 10
    successThreshold: 1
    failureThreshold: 12
